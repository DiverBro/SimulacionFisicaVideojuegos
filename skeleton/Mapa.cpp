#include "Mapa.h"

Mapa::Mapa(Vector3D pos, string file)
{
	/*RenderItem* render_Item = new RenderItem(CreateShape(PxBoxGeometry(1.0f)),
		new PxTransform(pos.getX(), pos.getY(), pos.getZ()), Vector4(1.0f, 0.0f, 0.0f, 1.0f));*/
}
